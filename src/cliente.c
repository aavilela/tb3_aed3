#include <stdio.h>
#include <stdlib.h>

#include "tab_hash.h"

int main(int argc, char *argv[])
{
	nomeDados = argv[1];

	AbreArquivos();
	
	//int *p = malloc(sizeof(int));
	//fread(p, sizeof(int), 1, fDados);

	//printf("%d\n", *p);
	return 0;
}
